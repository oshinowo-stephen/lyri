"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const fs_1 = require("fs");
const path_1 = require("path");
function loadSecrets(dir = '/run/secrets') {
    const secrets = fs_1.readdirSync(dir);
    const values = secrets.map((path) => fs_1.readFileSync(path_1.join(dir, path), 'utf-8'));
    return secrets.reduce((ax, dx, cx) => {
        ax[dx] = values[cx].trim();
        return ax;
    }, {});
}
exports.loadSecrets = loadSecrets;
//# sourceMappingURL=load-secrets.js.map